server.port=8080

# ===== DATABASE =====
spring.datasource.url=jdbc:mysql://localhost:3306/complaint_platform_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=admin
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===== EMAIL (SMTP via Gmail) =====
#
# HOW TO ENABLE REAL EMAIL SENDING:
#
# Step 1: Replace the two lines below with your actual Gmail credentials:
#   spring.mail.username = your Gmail address (e.g. yourname@gmail.com)
#   spring.mail.password = your Gmail App Password (NOT your normal Gmail password)
#
# Step 2: How to get a Gmail App Password:
#   Go to: https://myaccount.google.com/security
#   Enable 2-Step Verification (required)
#   Then go to: App passwords → select "Mail" → Generate
#   Copy the 16-character password and paste it below with NO spaces.
#
# Step 3: Change cms.email.enabled from false to true
#
# Until Step 3 is done, emails are safely printed to the Spring Boot console
# instead of being sent — so the approval workflow still works for testing.
#
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sbbraveena@gmail.com
spring.mail.password=clqvppjbmegyrrqf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# false = log emails to console (safe for development — no SMTP credentials needed)
# true  = send real emails via Gmail SMTP (requires valid credentials above)
cms.email.enabled=true
cms.upload.path=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
